# 学校部署规格与费用估算

以下价格按 2026-09-13 的公开价格和单校部署估算，仅用于预算规划。云服务器的实际费用会随
地区、带宽、磁盘类型、包年折扣和采购渠道变化，购买前应使用云厂商价格计算器重新核算。

## 服务器规格

系统会同时运行 Flask、Celery、Redis、本地 embedding、PDF/Word 解析和 PaddleOCR。
OCR 与 embedding 都会消耗较多 CPU 和内存，建议按使用规模选择：

| 使用规模 | 建议配置 | 适用场景 |
| --- | --- | --- |
| 100–300 人试点 | 4 核 CPU、16GB 内存、100GB SSD | 功能验证和少量日常使用 |
| 500–1500 人单校 | 8 核 CPU、32GB 内存、200GB SSD、10Mbps 带宽 | 推荐的正式上线起点 |
| 2000–5000 人 | 16 核 CPU、64GB 内存，数据库独立部署 | 课堂集中提问和批量 OCR |
| 多校使用 | 至少两台应用服务器、独立 PostgreSQL、Redis 和对象存储 | 高可用和横向扩容 |

一所约 1000 名学生的学校，基础设施可按以下月度范围预留：

| 项目 | 月费用估算 |
| --- | ---: |
| 8 核 32GB 应用服务器与 200GB 磁盘 | ¥800–1,800 |
| PostgreSQL、Redis、备份或额外数据盘 | ¥200–800 |
| 公网带宽 | ¥100–500 |
| 基础设施合计 | **¥1,100–3,100/月** |

若学校已有虚拟化服务器，基础设施的新增现金支出可以明显降低。国内云服务器可在
[阿里云 ECS 价格页](https://ecs-buy.aliyun.com/price)按部署地域和计费周期查询实际价格。

## LongCat API 费用

项目当前使用 `LongCat-2.0`。官方当前按量付费的限时价格为：未缓存输入 ¥2/百万 Token、
缓存输入 ¥0.04/百万 Token、输出 ¥8/百万 Token；原价分别为 ¥5、¥0.10 和 ¥20。
限时价格可能调整，应以 [LongCat 官方价格](https://longcat.chat/platform/docs/zh/pricing/longcat-2.0)
和最终账单为准。

一次辅导最多带入 5 个教材切片和最近 6 条对话。按平均输入 6,000 Token、输出 800 Token
估算，限时价格约为：

```text
6000 ÷ 1,000,000 × ¥2 + 800 ÷ 1,000,000 × ¥8 ≈ ¥0.018/次
```

| 每月辅导次数 | 限时价格估算 | 恢复原价后的估算 |
| ---: | ---: | ---: |
| 5,000 | ¥92 | ¥230 |
| 10,000 | ¥184 | ¥460 |
| 20,000 | ¥368 | ¥920 |
| 50,000 | ¥920 | ¥2,300 |

凭证字段提取和成长报告也会调用模型，单校可另外预留 ¥50–200/月。本地 embedding 和 OCR
不产生 API 费用，但会占用服务器算力。实际成本应以 Langfuse 或模型平台记录的输入、缓存输入
和输出 Token 为准。

## Langfuse 费用

Langfuse Cloud Hobby 计划免费，包含每月 50,000 units、30 天数据访问和 2 个用户；Core
计划为 $29/月，包含每月 100,000 units、90 天数据访问和无限用户，额外用量为
$8/100,000 units。试点可以先使用 Hobby，正式校内部署可按 Core 约 ¥210–300/月预留。
最新额度与教育优惠以 [Langfuse 官方价格](https://langfuse.com/pricing)为准。

综合服务器、LongCat 和 Langfuse，一所约 1000 名学生、每名学生每月约 10 次辅导的学校，
建议准备 **¥1,300–4,000/月** 的运行预算，不包含开发维护、域名、等保测评和人工运维成本。

## 正式上线前的必要改造

当前 Docker 后端使用 Flask 开发服务器和 SQLite，适合开发与演示，不能直接作为学校正式
生产环境。上线前至少应完成：

- 使用 Gunicorn 等生产 WSGI Server 运行 Flask。
- 将 SQLite 替换为 PostgreSQL，并为 embedding 增加 pgvector 索引。
- 使用 HTTPS，将 `SECRET_KEY` 和模型 API Key 放入密钥管理服务。
- 将上传文件迁移到私有对象存储或独立数据盘，配置病毒扫描、每日备份和恢复演练。
- 限制 CORS 来源，增加教师班级范围授权、操作审计和学生数据保留策略。
- 对发送给模型与 Langfuse 的学生姓名、成绩、问题、凭证文字进行脱敏，并确认服务商的
  数据保存范围、部署区域和学校合规要求。

生产部署后还应持续监控接口耗时、Celery 队列长度、磁盘空间、Token 消耗、模型错误率和
备份恢复结果，并根据实际峰值扩容。
